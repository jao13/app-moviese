import styled from 'styled-components/native';
import { theme } from '../../global/theme';

export const BackButtonTitleArea = styled.View`
  width: 100%;
  justify-content: space-between;
  align-items: center;
  flex-direction: row;
`;

export const BackButton = styled.TouchableOpacity`
  height: 26px;
  width: 26px;
`;